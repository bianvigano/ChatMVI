<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Profil • CHK</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    .wrap{max-width:560px;margin:24px auto;background:#fff;border:1px solid var(--border);border-radius:16px;box-shadow:var(--shadow);padding:18px}
    .f{margin-bottom:10px}
    .f label{display:block;margin-bottom:4px}
    .f input{width:100%;height:42px;border:1px solid var(--border);border-radius:10px;padding:10px 12px}
    .btn{height:42px;border-radius:10px;background:var(--brand);color:#fff;border:1px solid var(--brand-700);padding:0 14px;cursor:pointer}
    .err{color:#b91c1c;font-size:12px;margin-top:4px}
    .ok{background:#dcfce7;color:#166534;border:1px solid #86efac;padding:8px 10px;border-radius:8px;margin-bottom:10px}
  </style>
</head>
<body>
  <div class="wrap">
    <h2>Profil</h2>
    <% if (ok) { %><div class="ok">Profil diperbarui.</div><% } %>
    <form method="post" action="/profile">
      <div class="f">
        <label>Nama</label>
        <input name="name" value="<%= values.name || '' %>" />
        <% if (errors.name) { %><div class="err"><%= errors.name %></div><% } %>
      </div>

      <h3>Ganti Password (opsional)</h3>
      <div class="f">
        <label>Password Saat Ini</label>
        <input name="currentPassword" type="password" />
        <% if (errors.currentPassword) { %><div class="err"><%= errors.currentPassword %></div><% } %>
      </div>
      <div class="f">
        <label>Password Baru</label>
        <input name="newPassword" type="password" />
        <% if (errors.newPassword) { %><div class="err"><%= errors.newPassword %></div><% } %>
      </div>
      <div class="f">
        <label>Ulangi Password Baru</label>
        <input name="newPassword2" type="password" />
        <% if (errors.newPassword2) { %><div class="err"><%= errors.newPassword2 %></div><% } %>
      </div>

      <button class="btn" type="submit">Simpan</button>
    </form>
  </div>
</body>
</html>
