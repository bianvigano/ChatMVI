<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Daftar • CHK</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    .auth-wrap{max-width:520px;margin:40px auto;background:#fff;border:1px solid var(--border);border-radius:16px;box-shadow:var(--shadow);padding:18px}
    .auth-wrap h2{margin:0 0 10px}
    .f{margin-bottom:10px}
    .f label{display:block;margin-bottom:4px}
    .f input{width:100%;height:42px;border:1px solid var(--border);border-radius:10px;padding:10px 12px}
    .err{color:#b91c1c;font-size:12px;margin-top:4px}
    .row{display:flex;gap:10px}
    .row .f{flex:1}
    .btn{height:42px;border-radius:10px;background:var(--brand);color:#fff;border:1px solid var(--brand-700);padding:0 14px;cursor:pointer}
    .topbar{display:flex;align-items:center;gap:8px;padding:10px 12px;background:var(--brand);color:#fff}
    .topbar a{color:#fff;text-decoration:underline}
  </style>
</head>
<body>
  <div class="topbar">
    <div><b>CHK</b></div>
    <div class="muted">•</div>
    <div>Registrasi akun</div>
    <div style="margin-left:auto"><a href="/login">Sudah punya akun? Masuk</a></div>
  </div>
  <div class="auth-wrap">
    <h2>Daftar</h2>
    <form method="post" action="/register">
      <div class="f">
        <label>Nama pengguna</label>
        <input name="name" value="<%= values.name || '' %>" />
        <% if (errors.name) { %><div class="err"><%= errors.name %></div><% } %>
      </div>
      <div class="row">
        <div class="f">
          <label>Username</label>
          <input name="username" value="<%= values.username || '' %>" placeholder="huruf kecil/angka/_" />
          <% if (errors.username) { %><div class="err"><%= errors.username %></div><% } %>
        </div>
        <div class="f">
          <label>Email</label>
          <input name="email" type="email" value="<%= values.email || '' %>" />
          <% if (errors.email) { %><div class="err"><%= errors.email %></div><% } %>
        </div>
      </div>
      <div class="row">
        <div class="f">
          <label>Password</label>
          <input name="password" type="password" />
          <% if (errors.password) { %><div class="err"><%= errors.password %></div><% } %>
        </div>
        <div class="f">
          <label>Ulangi Password</label>
          <input name="password2" type="password" />
          <% if (errors.password2) { %><div class="err"><%= errors.password2 %></div><% } %>
        </div>
      </div>
      <button class="btn" type="submit">Buat Akun</button>
    </form>
  </div>
</body>
</html>
